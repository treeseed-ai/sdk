services:
  mailpit:
    image: axllent/mailpit:latest
    ports:
      - "${TREESEED_MAILPIT_SMTP_BIND:-127.0.0.1}:${TREESEED_MAILPIT_SMTP_PORT:-1025}:1025"
      - "${TREESEED_MAILPIT_UI_BIND:-127.0.0.1}:${TREESEED_MAILPIT_UI_PORT:-8025}:8025"
