schemaVersion: treeseed.guarantee/v1
id: guarantee.deployment.local-platform.start-the-managed-local-platform.571
journeyIndex: 571
type: deployment
subtype: local-platform
journey: "Start the managed local platform"
ownerPackage: "@treeseed/sdk"
surface: content-runtime
summary: "Guarantee that TreeSeed starts the managed local platform with independent agent and platform-operation provider identities, data roots, checkouts, manifests, and readiness checks."
status: planned
dependencies: { journeys: [], guarantees: [] }
actors: { allowed: [authenticated_user, team_owner, platform_admin], forbidden: [unauthorized_user] }
devices: { required: [desktop_chromium] }
gates: [future]
preconditions: { fixtures: [integrated_treeseed_project], notes: [] }
api: { required: false, verifierRefs: [] }
content: { required: false, verifierRefs: [] }
audit: { required: false, verifierRefs: [] }
negativeCases: []
evidence: { required: [guarantee_report] }
notes: ["A runtime generation is not ready unless every declared provider class has a fresh matching manager status and approved availability session."]
