schemaVersion: treeseed.guarantee/v1
id: guarantee.deployment.local-platform.initialize-platform-from-canonical-template.814
journeyIndex: 990
type: deployment
subtype: local-platform
journey: "Initialize a clean Platform clone from a canonical template"
ownerPackage: "@treeseed/sdk"
surface: cli
summary: "Clone an exact live Platform ref and validate an immutable Platform template without introducing a parallel profile schema or dirtying tracked source."
status: planned
dependencies: { journeys: [], guarantees: [guarantee.project.repository-federation.promote-exact-repository-receipts.813] }
actors: { allowed: [team_owner, platform_admin], forbidden: [unauthorized_user] }
devices: { required: [] }
gates: [future]
preconditions: { fixtures: [integrated_treeseed_project], notes: [] }
api: { required: false, verifierRefs: [] }
content: { required: false, verifierRefs: [] }
audit: { required: false, verifierRefs: [] }
negativeCases:
  - "Initialization rejects a wrong origin, moved ref, nonempty unrelated target, non-Platform template, unresolved external binding, or tracked managed-file divergence."
  - "Platform template payloads reject credentials, runtime state, logs, traces, screenshots, videos, rendered scenes, and generated evidence."
evidence: { required: [guarantee_report, repository_receipt] }
notes:
  - "Contract, initializer, and template validation are implemented. Keep planned until clean-clone live acceptance verifies exact remote ref, template digest, machine-local state, clean tracked source, runtime reconciliation, and workset replay."
