schemaVersion: treeseed.guarantee/v1
id: guarantee.project.repository-federation.promote-exact-repository-receipts.813
journeyIndex: 813
type: project
subtype: repository-federation
journey: "Promote an exact repository federation receipt"
ownerPackage: "@treeseed/sdk"
surface: cli
summary: "Guarantee that save records fresh live repository refs and stage/release consume that exact receipt without treating parent gitlinks as portfolio authority."
status: planned
dependencies: { journeys: [], guarantees: [] }
actors: { allowed: [team_owner, platform_admin], forbidden: [unauthorized_user] }
devices: { required: [] }
gates: [future]
preconditions: { fixtures: [integrated_treeseed_project], notes: [] }
api: { required: false, verifierRefs: [] }
content: { required: false, verifierRefs: [] }
audit: { required: false, verifierRefs: [] }
negativeCases:
  - "Stage is blocked when any receipt repository identity or live source ref moves."
  - "Release is blocked when any live staging ref differs from the receipt-derived stage candidate."
  - "Platform cannot encode Market, Market API, content repositories, or project gitlinks in its portfolio."
  - "Workset replay blocks dirty, divergent, nested, or incorrectly sourced existing repository paths without resetting them."
  - "A writable workset branch is blocked unless one non-expired acting assignment proves matching team, project, decision, accepted capacity plan, workday, repository, and exact base commit; unrelated repositories remain read-only."
  - "A governance execution authority is excluded when its repository identity, task branch, or integrated-commit ancestry does not match the saved repository."
  - "Stage fails closed when governed execution cannot be revalidated by the resolved control plane or when its decision, proposal revision, or upstream decision dependencies are stale."
evidence: { required: [guarantee_report] }
notes:
  - "Repository-scoped save defaults, explicit federated save, stale-ref rejection, receipt-derived stage candidates, live staging read-back, governed checkpoint trailers, complete authority/path coverage, control-plane decision freshness validation, persisted candidate evidence, a zero-gitlink live Platform portfolio, and clean-clone thirteen-repository workset, stage, close, release-plan, interruption, and recovery acceptance are implemented without `.gitmodules`. The guarantee remains planned until its verifier is registered and produces repeatable evidence."
  - "Platform-root pointer-update authority and journaled unpublished TreeDX authoring recovery are not yet covered; child workset authority must not be treated as proof of either contract."
