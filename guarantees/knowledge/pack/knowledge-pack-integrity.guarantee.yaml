schemaVersion: treeseed.guarantee/v1
id: guarantee.knowledge.pack.integrity.569
type: knowledge
subtype: pack
journey: Knowledge Pack Integrity
ownerPackage: "@treeseed/sdk"
surface: api-control-plane
summary: Require deterministic immutable packs built from exact TreeDX source closures with authorized selected-book scope and graph references.
status: active
actors: { allowed: [knowledge_pack_builder], forbidden: [unauthorized_user] }
gates: [core, security, release]
preconditions: { fixtures: [integrated_treeseed_project] }
api: { required: true, verifierRefs: [api.knowledge.pack.run-correlated, api.knowledge.collection.run-correlated] }
content: { required: true, verifierRefs: [sdk.knowledge.pack-integrity] }
audit: { required: false, verifierRefs: [] }
negativeCases:
  - { id: unauthorized-or-corrupt-pack-denied, actor: unauthorized_user, verifierRefs: [sdk.knowledge.pack-integrity, api.knowledge.pack.run-correlated] }
evidence: { required: [knowledge_pack_artifact, verifier_evidence] }
notes: [No-index release aggregate; deterministic SDK snapshots are paired with correlated operations-runner artifact and authorized download inspection.]
run: { requiredForRelease: true }
