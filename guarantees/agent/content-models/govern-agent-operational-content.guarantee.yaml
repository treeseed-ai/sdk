schemaVersion: treeseed.guarantee/v1
id: guarantee.agent.content-models.govern-agent-operational-content.913
journeyIndex: 913
type: agent
subtype: content-models
journey: Govern portable agent operational content
ownerPackage: "@treeseed/sdk"
surface: content-runtime
summary: Agent definitions, context queries, instructions, discussions, assignment records, tests, and evaluations use revision-safe SDK-owned portable content contracts.
status: planned
dependencies: { journeys: [], guarantees: [] }
actors: { allowed: [capacity_provider, authenticated_user], forbidden: [unauthorized_user] }
devices: { required: [], optional: [] }
gates: [future]
preconditions: { fixtures: [integrated_treeseed_project], notes: ["Agent content is accessed through TreeDX-backed operations."] }
api: { required: false, verifierRefs: [] }
content: { required: true, verifierRefs: [] }
audit: { required: true, verifierRefs: [] }
negativeCases:
  - "Legacy contentAccess and top-level permission policies cannot grant authority."
  - "A context query, instruction, test, or evaluation cannot substitute an unversioned reference."
  - "Provider overrides cannot widen the profile execution envelope."
evidence: { required: [guarantee_report, verifier_evidence] }
notes: ["Keep planned until TreeDX-backed round-trip and provider-runtime verifier evidence is repeatable."]
