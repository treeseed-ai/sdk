schemaVersion: treeseed.guarantee/v1
id: guarantee.content.projects-templates.validate-template-compatibility.640
journeyIndex: 640
type: content
subtype: projects-templates
journey: "Validate template compatibility"
ownerPackage: "@treeseed/sdk"
surface: content-runtime
summary: "Validate template and other authored content against the canonical portable Zod registry before publication."
status: planned
dependencies: { journeys: [], guarantees: [] }
actors: { allowed: [authenticated_user, team_owner, platform_admin], forbidden: [unauthorized_user] }
devices: { required: [desktop_chromium] }
gates: [future]
preconditions: { fixtures: [integrated_treeseed_project], notes: [] }
api: { required: false, verifierRefs: [] }
content: { required: false, verifierRefs: [] }
audit: { required: false, verifierRefs: [] }
negativeCases: [{ id: invalid-model-blocks-publication, actor: authenticated_user, verifierRefs: [] }]
evidence: { required: [guarantee_report] }
notes: ["The SDK publication reconciler now rejects known-model frontmatter before runtime-manifest or R2 work and returns exact path, model, field, code, and message diagnostics. Keep planned until the verifier is registered and repeatable portfolio evidence is attached."]
