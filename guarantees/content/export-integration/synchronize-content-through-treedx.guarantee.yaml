schemaVersion: treeseed.guarantee/v1
id: guarantee.content.export-integration.synchronize-content-through-treedx.659
journeyIndex: 659
type: content
subtype: export-integration
journey: "Synchronize content through TreeDX"
ownerPackage: "@treeseed/sdk"
surface: content-runtime
summary: "Synchronize model-valid content through TreeDX without allowing invalid reads or writes to cross the SDK boundary."
status: planned
dependencies: { journeys: [], guarantees: [] }
actors: { allowed: [authenticated_user, team_owner, platform_admin], forbidden: [unauthorized_user] }
devices: { required: [desktop_chromium] }
gates: [future]
preconditions: { fixtures: [integrated_treeseed_project], notes: [] }
api: { required: false, verifierRefs: [] }
content: { required: false, verifierRefs: [] }
audit: { required: false, verifierRefs: [] }
negativeCases:
  - { id: invalid-read-fails-with-field-diagnostics, actor: authenticated_user, verifierRefs: [] }
  - { id: invalid-write-makes-no-workspace-mutation, actor: authenticated_user, verifierRefs: [] }
evidence: { required: [guarantee_report] }
notes: ["The generic SDK TreeDX backend validates every first-party portable model on read and immediately before write, preserves exact path/model/field diagnostics, and does not hide invalid direct reads as missing records. Workday demand, planning-artifact projection, knowledge parsing, and governance authoring use the same portable boundary. Keep planned until the verifier is registered and repeatable live evidence is attached."]
