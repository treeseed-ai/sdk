# Team Project Portfolio Demo

This fixture documents the central TreeSeed acceptance test harness and demo / educational video generator.

Run with `--device desktop`, `--device tablet`, `--device mobile`, or `--device all`.

The generator clears the local Mailpit inbox, demonstrates Mailpit confirmation, and captures the SDK linked software project requirement.
